/*Crie uma constante numérica. 
Utilizando o operador ternário 
(condicao ? valor1 : valor2),
determine se o número é par ou ímpar e guarde o 
resultado em uma variável de texto para
exibir no console.
*/

const valor = 14;

const par = valor % 2 === 0;

alert(par ? 'Par' : 'Impar')
